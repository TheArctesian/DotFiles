if status is-interactive
    # Commands to run in interactive sessions can go here
end

alias ls "lsd -l"
alias la "ls -a"
alias lla "ls -la"
alias lt  "ls --tree"
alias vim "nvim"
alias cls "clear" # this is the windows way of doing it that is easier
alias p "ping -c 3 gentoo.org"
alias clok "tty-clock -c -C 4 -S"
alias py "python3" # this is the windows way of doing it that is easier
