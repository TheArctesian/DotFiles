# Legacy configurations

This directory preserves the repository's previous multi-platform layout,
including Linux distributions, window managers, desktop environments, old Mac
setup scripts, vendored projects, wallpapers, and earlier copies of the active
configs.

Nothing in this directory is installed by the root `install.sh`. Treat it as a
reference archive while the current macOS setup lives under the root
`.config/` directory.
